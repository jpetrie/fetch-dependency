set(FETCH_DEPENDENCY_PACKAGE_NAMES "${FETCH_DEPENDENCY_PACKAGE_NAMES}")
set(FETCH_DEPENDENCY_PACKAGE_PATHS "${FETCH_DEPENDENCY_PACKAGE_PATHS}")

if(NOT ${ED_NO_FINDPACKAGE})
  list(APPEND CMAKE_PREFIX_PATH "${FETCH_DEPENDENCY_PACKAGE_PATHS}")
endif()

if(NOT ${ED_NO_FINDPACKAGE})${FindPackageCalls}
endif()

